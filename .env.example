TELEGRAM_BOT_TOKEN=
# Auth: run 'claude login' to authenticate with your Max subscription
DEEPGRAM_API_KEY=
ALLOWED_USER_IDS=
DEFAULT_WORKING_DIR=/home/user
SQLITE_PATH=./data/vibegram.db
LOG_LEVEL=info

# Server configuration (matches VibeTunnel's default port for ngrok compatibility)
PORT=4020
BIND=0.0.0.0

# Webhook mode (optional - for ngrok tunneling)
# USE_WEBHOOK=true
# WEBHOOK_URL=https://your-ngrok-url.ngrok.io
# WEBHOOK_PATH=/webhook
